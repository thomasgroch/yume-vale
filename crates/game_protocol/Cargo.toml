[package]
name = "game_protocol"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
bevy = { workspace = true, features = ["3d", "serialize", "bevy_state"] }
lightyear = { workspace = true, features = ["client", "server", "std", "replication", "interpolation"] }
serde = { workspace = true }
game_core = { workspace = true }

[dev-dependencies]
serde_json = "1"
bevy_replicon = "0.41"
